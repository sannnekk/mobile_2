import 'package:flutter/material.dart';

class AssignedWorksPage extends StatelessWidget {
  const AssignedWorksPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Assigned Works Page'));
  }
}
